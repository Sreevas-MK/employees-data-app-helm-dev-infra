apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  labels:
    type: local
spec:
  storageClassName: {{ .Values.global.storageclass }}
  capacity:
    storage: {{ .Values.redis.storage }}
  persistentVolumeReclaimPolicy: Retain  # persistentVolumeReclaimPolicy: Retain, If you delete the PVC, data stays on host.
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.redis.storagepath }}
    type: DirectoryOrCreate

  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: {{ .Values.global.nodeSelector.label }}
          operator: In
          values:
          - {{ .Values.redis.nodename }}
